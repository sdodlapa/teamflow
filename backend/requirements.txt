# Complete production dependencies for TeamFlow Backend
# Systematically analyzed from codebase imports

# Core FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database and ORM
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1
cryptography==41.0.7

# Validation and serialization
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.3.0

# Environment and configuration
python-dotenv==1.0.0

# HTTP client and networking
httpx==0.25.2
aiofiles==23.2.1

# Templating (for Jinja2 usage)
jinja2==3.1.2

# Image processing and file handling
pillow==10.1.0
# TODO LATER: Re-enable after configuring libmagic system dependency in nixpacks.toml
# python-magic==0.4.27  # Requires system libmagic library

# System monitoring and performance
psutil==5.9.6

# Caching and performance (Redis)
redis==5.0.1
aioredis==2.0.1

# YAML processing (if needed)
pyyaml==6.0.1

# WebSocket support
websockets==12.0

# Compression
brotli==1.1.0